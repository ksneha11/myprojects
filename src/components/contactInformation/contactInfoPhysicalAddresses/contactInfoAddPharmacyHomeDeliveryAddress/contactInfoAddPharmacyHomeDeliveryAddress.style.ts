import { TextStyle, ViewStyle } from 'react-native';
import { StyleSchemaParams, StyleSheetSchema } from '../../../../styles';

export interface ContactInfoAddPharmacyHomeDeliveryAddressStyleSchema extends StyleSheetSchema {
    rootContainer: ViewStyle;
}

export default ({ colorSchema, windowWidth, hairlineWidth }: StyleSchemaParams): ContactInfoAddPharmacyHomeDeliveryAddressStyleSchema => {
    return {
        rootContainer: {},
    };
};
